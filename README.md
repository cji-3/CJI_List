# C語言可變長列表函式庫

## 版本：1.0.0

### function title

CLS\_CreateList(DataByte)  創建list

CLS\_AddLast(list,data)  在後面加入元素

CLS\_AddIndex(list,index,data)  在指定索引位置新增(插入)

CLS\_Replace(list,index,data)  替換某元素

CLS\_ReadIndex(list,index)  讀取list中的元素

CLS\_DelIndex(list,index)  刪除list中的元素
